services:
  news-crawler:
    build:
      context: .
    env_file:
      - path: .env
        required: false
    ports:
      - "3000:3000"
    volumes:
      - ./data:/app/data
    restart: unless-stopped
