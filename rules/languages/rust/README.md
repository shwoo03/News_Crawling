# Rust Rules Pack

- Prefer explicit error types for library boundaries and `anyhow`-style context for binaries.
- Use `cargo test` and `cargo clippy` when the project has `Cargo.toml`.
- Avoid broad rewrites of public APIs without an ADR or plan update.
