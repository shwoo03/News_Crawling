# Python 규칙 시드

최소 시드입니다. 프로젝트가 실제로 Python을 사용할 때만 이 파일을 확장합니다.

- 의존성은 명시적·재현 가능하게 관리합니다.
- 전역 환경 대신 프로젝트 지역 환경(`venv`, `uv`, `poetry`)을 사용합니다.
- 프로젝트에서 설정됐다면 `black`으로 포맷합니다.
- 프로젝트에서 설정됐다면 `ruff`로 린트합니다.
- 프로젝트에서 설정됐다면 동작 변경을 주장하기 전에 가장 좁은 범위의 `pytest`를 실행합니다.
- 실패한 체크를 문서화된 이유 없이 억제하지 않습니다.
