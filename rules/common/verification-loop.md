# 검증 루프 규칙

검증되기 전에는 완료를 성공으로 보지 않습니다.

최소 검증 항목:

- 필요한 파일이나 산출물이 존재하는지 확인합니다.
- JSON, JSONL, YAML, TOML 같은 구조적 파일이 파싱 가능한지 확인합니다.
- 가능한 경우 관련 테스트나 정적 체크를 실행합니다.
- 문서가 실제 경로를 가리키는지 확인합니다.
- 중요한 검증 결과는 runtime 로그에 남깁니다.

검증을 실행할 수 없다면 그 원인을 명시적으로 설명합니다.
