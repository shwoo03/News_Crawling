name: PR Standards

on:
  pull_request:

jobs:
  standards:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check skeleton gate
        run: python3 scripts/verify.py --skip-unit
